// Integer check karna

console.log(Number.isInteger(10));      // true, kyunki 10 poora number hai
console.log(Number.isInteger(10.5));    // false, decimal number hai
console.log(Number.isInteger("10"));    // false, string hai
